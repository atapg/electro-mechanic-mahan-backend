const routes = require('express').Router()
// const upload = require('../utils/multer')

// routes.post('/images', upload.array('images'), (req, res) => {
//     try {
//         res.json({
//             status: 'Success',
//             uploaded: true,
//             url: req.files,
//         })
//     } catch (error) {
//         res.json({
//             status: 'Failed',
//             error,
//         })
//     }
// })

module.exports = routes
